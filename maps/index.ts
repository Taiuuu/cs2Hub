import { dust2Data } from "./dust2";
import { infernoData } from "./inferno";
import { mirageData } from "./mirage";
import { nukeData } from "./nuke";
import { ancientData } from "./ancient";
import { anubisData } from "./anubis";
import { cacheData } from "./cache";
import { overpassData } from "./overpass";
import { vertigoData } from "./vertigo";
import { trainData } from "./train";

export const dust2Map = dust2Data;
export const infernoMap = infernoData;
export const mirageMap = mirageData;
export const nukeMap = nukeData;
export const ancientMap = ancientData;
export const anubisMap = anubisData;
export const cacheMap = cacheData;
export const overpassMap = overpassData;
export const vertigoMap = vertigoData;
export const trainMap = trainData;

export const allMaps = [
  dust2Map,
  infernoMap,
  mirageMap,
  nukeMap,
  ancientMap,
  anubisMap,
  cacheMap,
  overpassMap,
  vertigoMap,
  trainMap,
];